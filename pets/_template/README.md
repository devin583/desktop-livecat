# Desktop LiveCat Resource Pack Template

Copy this folder to `pets/<your-pet-id>/`, remove the leading underscore, and
replace the placeholder files.

Use this when giving the project to an illustrator. They only need the files in
`artist/`, plus a preview image. Engineering can complete the runtime model and
manifest details later.
