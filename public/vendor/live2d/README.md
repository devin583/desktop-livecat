# Live2D Cubism SDK slot

This directory is reserved for locally supplied Live2D Cubism SDK for Web runtime files.

The app treats Live2D as the primary renderer, but the SDK itself is not committed because
distribution must follow Live2D's release license. Put approved runtime files here during
local packaging, or point a pet pack at an already licensed runtime in a future loader update.
